import SwiftUI
import shared

func foo(authenticationService: AuthenticationService) async throws {
}

@main
struct KontinuitySampleApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
